import axios from 'axios';

const BASE_API_URL_POKEMON = 'https://pokeapi.co/api/v2/pokemon';

export const getPokemons = async () => {
  try {
  } catch (e) {
    throw new Error({ error: e.message });
  }
};
